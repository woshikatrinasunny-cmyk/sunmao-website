# Render 部署指南

## 准备工作

### 1. 创建 GitHub 仓库

1. 在 GitHub 上创建一个新仓库（例如：`sunmao-website`）
2. 在本地项目文件夹中初始化 Git：

```bash
git init
git add .
git commit -m "Initial commit: 榫卯文化旅游网站"
git branch -M main
git remote add origin https://github.com/你的用户名/sunmao-website.git
git push -u origin main
```

### 2. 注册 Render 账号

1. 访问 [https://render.com](https://render.com)
2. 使用 GitHub 账号注册/登录
3. 授权 Render 访问你的 GitHub 仓库

## 部署步骤

### 方法一：使用 render.yaml（推荐）

1. **连接仓库**
   - 在 Render Dashboard 点击 "New +"
   - 选择 "Blueprint"
   - 连接你的 GitHub 仓库
   - Render 会自动检测 `render.yaml` 文件

2. **配置服务**
   - 服务名称：sunmao-website（或自定义）
   - 环境：Python 3
   - 构建命令：`pip install -r requirements.txt`
   - 启动命令：`gunicorn app:app`

3. **部署**
   - 点击 "Apply" 开始部署
   - 等待 5-10 分钟完成构建和部署

### 方法二：手动创建 Web Service

1. **创建新服务**
   - 在 Render Dashboard 点击 "New +"
   - 选择 "Web Service"
   - 连接你的 GitHub 仓库

2. **配置设置**
   ```
   Name: sunmao-website
   Environment: Python 3
   Region: Singapore (或选择离你最近的)
   Branch: main
   Build Command: pip install -r requirements.txt
   Start Command: gunicorn app:app
   ```

3. **高级设置（可选）**
   - Instance Type: Free（免费套餐）
   - Auto-Deploy: Yes（自动部署）

4. **创建服务**
   - 点击 "Create Web Service"
   - 等待部署完成

## 部署后

### 访问网站

部署成功后，Render 会提供一个 URL，格式如下：
```
https://sunmao-website.onrender.com
```

### 自定义域名（可选）

1. 在 Render Dashboard 中选择你的服务
2. 进入 "Settings" 标签
3. 在 "Custom Domain" 部分添加你的域名
4. 按照提示配置 DNS 记录

### 环境变量（如需要）

如果需要添加环境变量：
1. 进入服务的 "Environment" 标签
2. 添加环境变量，例如：
   ```
   FLASK_ENV=production
   SECRET_KEY=your-secret-key
   ```

## 常见问题

### 1. 部署失败

**检查日志**：
- 在 Render Dashboard 查看 "Logs" 标签
- 查找错误信息

**常见原因**：
- `requirements.txt` 文件格式错误
- Python 版本不兼容
- 依赖包安装失败

**解决方法**：
```bash
# 本地测试依赖安装
pip install -r requirements.txt

# 确保 requirements.txt 格式正确
Flask==3.0.0
gunicorn==21.2.0
```

### 2. 网站无法访问

**检查服务状态**：
- 确保服务状态为 "Live"（绿色）
- 查看 "Events" 标签了解部署历史

**检查端口配置**：
- Render 会自动设置 `PORT` 环境变量
- 确保 `app.py` 中使用了 `os.environ.get('PORT', 5001)`

### 3. 静态文件无法加载

**检查文件路径**：
- 确保 `static/` 和 `templates/` 文件夹在仓库中
- 检查 `.gitignore` 没有排除这些文件夹

**添加静态文件到 Git**：
```bash
git add static/
git add templates/
git commit -m "Add static files"
git push
```

### 4. 图片显示为占位符

这是正常的，因为实际图片需要你手动添加：

1. 准备图片文件（参考 `图片说明.txt`）
2. 将图片放入 `static/images/` 文件夹
3. 提交并推送到 GitHub：
```bash
git add static/images/
git commit -m "Add product images"
git push
```
4. Render 会自动重新部署

### 5. 免费套餐限制

Render 免费套餐的限制：
- 15 分钟无活动后服务会休眠
- 首次访问可能需要等待 30-60 秒唤醒
- 每月 750 小时免费运行时间
- 100GB 带宽

**解决休眠问题**：
- 升级到付费套餐（$7/月起）
- 使用 UptimeRobot 等服务定期 ping 你的网站

## 更新网站

### 自动部署

如果启用了 Auto-Deploy：
```bash
# 修改代码后
git add .
git commit -m "Update website"
git push
```

Render 会自动检测更改并重新部署。

### 手动部署

在 Render Dashboard 中：
1. 选择你的服务
2. 点击 "Manual Deploy"
3. 选择 "Deploy latest commit"

## 监控和维护

### 查看日志

```bash
# 在 Render Dashboard 的 "Logs" 标签查看实时日志
```

### 性能监控

Render 提供基本的性能指标：
- CPU 使用率
- 内存使用率
- 请求响应时间

### 备份

定期备份你的代码：
```bash
git push origin main
```

## 成本估算

- **免费套餐**：$0/月
  - 适合个人项目和演示
  - 有休眠限制

- **Starter 套餐**：$7/月
  - 无休眠
  - 更好的性能
  - 适合小型生产环境

## 技术支持

- Render 文档：https://render.com/docs
- Render 社区：https://community.render.com
- GitHub Issues：在你的仓库创建 issue

## 下一步

部署成功后，你可以：

1. ✅ 添加真实的图片文件
2. ✅ 配置自定义域名
3. ✅ 添加 Google Analytics 等分析工具
4. ✅ 优化 SEO（添加 meta 标签）
5. ✅ 添加更多功能（用户登录、评论系统等）

祝部署顺利！🎉
